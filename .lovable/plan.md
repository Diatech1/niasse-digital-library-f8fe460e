Update the `preferred_voice` for the `rihla-kunakiriya` row in the `books` table from `Schedar` to `Orus` (deep, firm). No code changes needed — the per-book voice override plumbing is already in place.